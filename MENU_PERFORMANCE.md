# 菜单首次点击与存档性能优化（2026-09-12，P62）

后续 P63 的位图分配、字体生命周期和选项居中修复见 [FREEZE_DIAGNOSIS.md](FREEZE_DIAGNOSIS.md)。下文的 22,151 项校验、性能数字及 NRO 哈希均是 P62 当时记录；当前扩展后的测试为 22,813 项，比较新版结果需传入 `--after-checks 22813`（两边均为新版时再加 `--before-checks 22813`）。

目标是减少进入游戏后首次打开菜单、切换设置和快速存档时主线程上的集中工作。检查范围包括事件循环、脚本加载、字体、图像缓存、PSB、XP3/7z、存档与现有渲染路径。修改作用于公共引擎路径，没有按游戏名注入逻辑。

## 改动与边界

| 路径 | 原有开销 | 本次实现 |
|---|---|---|
| FreeType 字号度量缓存 | 每个新字号首次使用初始化 65,535 个条目，约 6.25 MiB | 256 字符一页，按实际使用分配，支持完整 BMP，包括 U+FFFF；保留字号隔离和引用稳定性 |
| 文本/结构化存档 | 每次小片段都分配、拷贝 UTF-16，再进入 zlib 或文件写入 | 原生 UTF-16LE 直接送入 16 KiB 缓冲，关闭时先刷新再完成压缩与回填长度；保留加密模式、压缩等级和异常传播 |
| PSB 资源 | 解包、资源枚举、媒体注册和打开流多次复制完整资源 | 原始数据共享只读所有权，媒体用切片；TJS octet 仍保留独立值语义；旧流在容器重载/缓存释放后仍有效 |
| PSB 字典和文本 | 大字典使用默认 8 个哈希桶，相同字符串索引反复分配 TJS 字符串 | 使用现有 `Dictionary.assign` 同类的 `RebuildHash(count)` 预留容量；共享 TJS 写时复制字符串；实例和字段修改互不影响 |
| 固实 7z | 每次打开成员都重新解压整块 | 所有归档共用一个最近使用的块缓存，最多保留 16 MiB；每次读取仍检查边界及成员 CRC；打开流独立持有内存 |
| 图像缓存 | 同键替换重复计费；插入前检查预算，插入后可超额 | 替换减去旧尺寸、插入后执行淘汰；超预算单图不驱逐整个热缓存；缓存命中免去路径诊断格式化 |
| 性能记录 | 60 帧平均值掩盖单次卡顿 | 超过 16 ms 的脚本、图像、PSB、文本存档、图像存档记录 `[slow]`；单次主循环活动超过 100 ms 记录 `[stall]` 的事件/派发/绘制分段 |

TJS 执行和同步存档仍遵守原本的调用顺序。7z 超过 16 MiB 的块仍需按原方式解压；正在读取的流占用的内存不属于可淘汰缓存。分页字体的 27 KiB 是**一个新字号仅使用第一页**的分配模型，多页中文文本会继续按需增加。渲染、字体栅格化、压缩等级和画面质量未作降级。

## 验证方法

1. `cmake --build build-tests --config Release`、`ctest --test-dir build-tests -C Release --output-on-failure`：`menu_perf`、`bitmap_bridge`、`emote_gl`、`emote_cpu` 共 4 项通过。
2. `menu_perf_test` 检查全部 65,536 个字符槽位、不同字号、缓存引用稳定性、共享流生命周期/越界/寻址溢出、写入合并与失败传播，共 65,555 项断言。桌面分配模型为 6,553,500 → 27,648 字节；20,004 次片段写入合成 8 次底层写入，179,152 字节输出一致。
3. `tests/build_menu_perf_fixture.py` 生成独立 PSB/MDF、10,000 键字典、4 MiB 固实 7z 和测试脚本，替换**NRO 副本**的 RomFS。可执行代码、图标、NACP 不变，不使用游戏美术资源。对优化前后完整 ARM 引擎使用同一脚本运行。
4. 脚本对 31 个字号、六种存档模式、Unicode/嵌套值、PSB 重载与失效后的资源、共享字符串独立修改、32 个归档成员共做 22,151 项校验。结果在测试环境 `sdmc:/switch/KRKR-ns/perf-test/`，与游戏存档目录分离。
5. `tests/compare_menu_perf_results.py BEFORE AFTER` 核对完成标志、字体宽度和存档有效内容。压缩模式还核对头部长度和解压长度；`z0` 允许因写入批次变化产生不同的压缩块边界。

复现命令（PowerShell，依赖本地 devkitPro、CMake/SDL2 和 7-Zip）：

```powershell
& 'C:\Program Files\Git\bin\bash.exe' ./build_nro.sh --no-emu-copy
python tests/build_menu_perf_fixture.py --nro .zcode/menu-perf/baseline.nro --output-dir .zcode/menu-perf/before-final
python tests/build_menu_perf_fixture.py --nro build-switch/krkrsdl2.nro --output-dir .zcode/menu-perf/after-final
# 分别运行两个 menu-perf.nro，完成后将 perf-test 目录复制为两个结果目录。
python tests/compare_menu_perf_results.py .zcode/menu-perf/before-final-results .zcode/menu-perf/after-final-results
```

## 运行记录

以下为最终版本与修改前 NRO 各一次运行的总耗时（ms），Ryujinx Nextendo 1.8.3，同一套合成数据。模拟器耗时只用于判断代码路径是否减少工作，不能换算成 Switch 真机点击延迟。真机未连接，尚无本版本真机性能结论。

| 测试场景 | 修改前 | 修改后 |
|---|---:|---:|
| 31 个字号首次计算宽度 | 316 | 311 |
| 同样字号再次计算 | 19 | 23 |
| 文本存档 | 40 | 37 |
| 加密文本存档 c1 | 21 | 13 |
| 压缩存档 z | 37 | 30 |
| 压缩存档 z0 | 16 | 11 |
| 压缩存档 z9 | 39 | 33 |
| 二进制存档 b | 13 | 14 |
| PSB 10,000 键字典首次解析 | 102 | 29 |
| 小型资源 PSB 连续加载 12 次 | 36 | 44 |
| PSB 图片资源打开 32 次 | 40 | 41 |
| 固实 7z 连续打开 32 个成员 | 4,539 | 269 |

大字典解析约减少 72%，固实归档场景约减少 94%；不能推广为所有菜单都按此比例改善。小型 PSB 的复制减少没有在本轮表现出耗时收益，字体全流程也未取得明显提速：字体优化已确认的收益主要是首次分配量下降。保留全部结果，后续用真机测量区分实际收益和计时波动。

两版各通过 22,151 项校验；31 个文字宽度一致，六种存档的有效内容一致。`save-0/1/2/4/5.ksd` 字节完全一致，`save-3.ksd`（z0）解压后完全一致。完整测试视口 `(12,98)-(2008,1215)` 的截图逐像素一致。

真实《星光咖啡馆与死神之蝶》检查覆盖快速读档页、读档确认、剧情文本、快速存档确认及写入后的新缩略图、设置页、文本设置页和返回游戏。测试使用模拟器存档的备份；结束后恢复原文件。真实游戏日志仍有全屏设置和音频 `WaveSoundBuffer.open` 相关兼容性异常，本轮不能据此声称游戏兼容性已全部通过；公共测试场景没有脚本异常。

原始构建、日志、截图与结果保存在本地 `.zcode/menu-perf/`。旧 NRO 保留为 `baseline.nro`，SHA256 为 `CB8A365B83FDC6089FFB175698A3CF14B1911730B4D0390CC1F5DC54C362E485`。

正式 NRO：27,970,649 字节；SHA256 `4710600CCE30E7C6AB8456571120613617D01CD7F0965A9AE49EFB25E6641401`。其 RomFS 保留正常游戏启动器，不含性能测试脚本或生成的测试资源。

## 真机复验

正式交付使用 `build-switch/krkrsdl2.nro`，不要将测试目录中的 `menu-perf.nro` 当作游戏启动器。按原安装方式替换 NRO 并保留游戏与存档目录。冷启动后进入同一存档，依次首次打开快速存档确认框、存读档页、设置页、历史页，再各重复一次；记录冷/热两轮差异。日志位于 `sdmc:/switch/KRKR-ns/log/krkrsdl2_debug_<时间戳>.log`，保留最新 3 次启动。

若仍有明显停顿，用同次点击附近的 `[slow]` 与 `[stall]` 确定剩余耗时属于解码、序列化、脚本还是绘制，再按真实数据继续优化。
